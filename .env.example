# Postgres
POSTGRES_DB=strapi
POSTGRES_USER=strapi
POSTGRES_PASSWORD=strapi

# Strapi / app
HOST=0.0.0.0
PORT=1337
DATABASE_CLIENT=postgres
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=${POSTGRES_DB}
DATABASE_USERNAME=${POSTGRES_USER}
DATABASE_PASSWORD=${POSTGRES_PASSWORD}

# Security keys (replace with secure generated values for production)
APP_KEYS=replace_this_with_comma_separated_app_keys
API_TOKEN_SALT=replace_with_random_string
ADMIN_JWT_SECRET=replace_with_random_string
JWT_SECRET=replace_with_random_string

# Optional: provide full DATABASE_URL instead of host/port
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified
